<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.test">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".InitialPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PrivatePage"
            android:label="@string/app_name" />
        <activity
            android:name=".SearchPage"
            android:label="@string/title_activity_search_page"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".SettingPage" />
        <activity android:name=".RegisterPage" />
        <activity android:name=".FeedbackPage" />
        <activity android:name=".PublicPage" />
        <activity android:name=".LoginPage" />
        <activity android:name=".DetailPage" />
        <activity android:name=".AccountSetting" />
        <activity android:name=".CreateActivity" />
        <activity android:name=".LabelForEvent" />
        <activity android:name=".SetLabelForUser" />
        <activity android:name=".ContactUs" />
        <activity android:name=".OurInfo"></activity>
    </application>

</manifest>